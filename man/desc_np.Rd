% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc-nonparam.R
\name{desc_np}
\alias{desc_np}
\title{Non-parametric NPMLE summary of interval-censored data}
\usage{
desc_np(x, method = c("turnbull", "wang"))
}
\arguments{
\item{x}{A \code{cens_data} object (see \code{\link[=as_interval_data]{as_interval_data()}}).}

\item{method}{NPMLE algorithm. \code{"turnbull"} (default) uses
\code{\link[survival:survfit]{survival::survfit()}} with \code{type = "interval2"}; \code{"wang"} uses
\code{\link[icenReg:ic_np]{icenReg::ic_np()}} (the EMICM algorithm).}
}
\value{
A \code{cens_np_fit} object: a list with the fitted \code{fit}, the \code{method}
and \code{backend} used, \code{log_transform}, the number of observations \code{n}, the
number of rows \code{dropped} for missing bounds, and the \code{category} counts.
}
\description{
Fits a non-parametric maximum-likelihood estimate (NPMLE) of the distribution
underlying a \code{cens_data} object, using the Turnbull estimator. This is the
distribution-free descriptive summary for the three-tier interval-censoring
structure: it needs no parametric assumption about the concentration
distribution and correctly accounts for non-detects and
detected-but-not-quantified observations.
}
\details{
The estimate is computed on the \strong{original measurement scale}. Because the
Turnbull NPMLE depends only on the ordering and overlap of the intervals, it
is invariant to the monotone log transform stored in the \code{cens_data} object;
fitting on the original scale simply means the resulting quantiles are
reported directly as concentrations.
}
\examples{
x <- as_interval_data(left = c(0, 1, 5, 8), right = c(1, 3, 5, 8))
fit <- desc_np(x)
quantile(fit)
}
\seealso{
\code{\link[=quantile.cens_np_fit]{quantile.cens_np_fit()}} for quantile extraction and
\code{\link[=plot.cens_np_fit]{plot.cens_np_fit()}} for the cumulative-distribution plot.
}
