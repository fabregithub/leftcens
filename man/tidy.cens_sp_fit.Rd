% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.cens_sp_fit}
\alias{tidy.cens_sp_fit}
\title{Tidy a semi-parametric fit}
\usage{
\method{tidy}{cens_sp_fit}(x, exponentiate = FALSE, ...)
}
\arguments{
\item{x}{A \code{cens_sp_fit} object.}

\item{exponentiate}{If \code{TRUE}, report \code{estimate} on the ratio scale (hazard
ratio for \code{"ph"}, odds ratio for \code{"po"}).}

\item{...}{Unused.}
}
\value{
A data frame with \code{term}, \code{estimate}, and (when the fit was built with
\code{bs_samples > 0}) \code{std.error}, \code{statistic}, and \code{p.value}.
}
\description{
Returns the covariate coefficient table (one row per term).
}
\examples{
x <- as_interval_data(c(0, 1, 5, 8, 0, 2), c(1, 3, 5, 8, 1, 4))
grp <- factor(c("a", "a", "a", "b", "b", "b"))
tidy(desc_sp(x, covariates = grp, model = "ph"))
}
