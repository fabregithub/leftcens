% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-conditional.R
\name{impute_censored_conditional}
\alias{impute_censored_conditional}
\title{Impute a censored variable conditional on a general design matrix}
\usage{
impute_censored_conditional(
  y,
  x,
  lower,
  upper,
  m = 1L,
  margin = c("shash", "gaussian"),
  proper = TRUE
)
}
\arguments{
\item{y}{Numeric vector; \code{NA} at the cells to impute (censored or missing),
the observed value elsewhere.}

\item{x}{Predictors: a matrix or data frame with one row per element of \code{y}
(an outcome, covariates, other analytes; mixed types allowed). No intercept
column is needed -- one is added.}

\item{lower, upper}{Per-cell censoring bounds, same length as \code{y}, used only at
the \code{NA} cells: the imputed value is drawn within \verb{(lower, upper]}. For a
left-censored non-detect at limit \code{L} use \code{lower = -Inf}, \code{upper = L}; for an
interval use both finite bounds. Values at observed cells are ignored.}

\item{m}{Number of completed vectors to return.}

\item{margin}{\code{"shash"} (default, skew-aware sinh-arcsinh margin) or
\code{"gaussian"} (a plain tobit conditional; use when skew is known negligible).}

\item{proper}{If \code{TRUE} (default), draw the margin and regression parameters
from their posteriors per imputation, so between-imputation variance is
honest. \code{FALSE} plugs in the MLEs (single-imputation / point use).}
}
\value{
A numeric matrix with \code{length(y)} rows and \code{m} columns: each column is
a completed copy of \code{y} (observed cells carried through unchanged, censored
cells imputed).
}
\description{
Multiple imputation of a single left-/interval-censored variable, conditioning
on an arbitrary predictor matrix and respecting the per-cell censoring bounds.
Skew is handled by a sinh-arcsinh margin so non-detects are drawn from a
correctly shaped tail; the conditional mean is modelled linearly on the latent
normal scale. This is the general-predictor complement to
\code{\link[=gsimp_impute]{gsimp_impute()}} (which conditions on other analytes only) and the reusable
core for FCS / \code{mice} integration.
}
\examples{
set.seed(1)
n <- 300
z  <- rnorm(n)
x1 <- 0.6 * z + rnorm(n)                     # a covariate-correlated exposure
lod <- quantile(x1, 0.3)
y  <- x1                                     # observed values
y[x1 < lod] <- NA                            # left-censored below the LOD
lower <- ifelse(is.na(y), -Inf, NA)
upper <- ifelse(is.na(y), lod,  NA)
imp <- impute_censored_conditional(y, x = data.frame(z), lower, upper, m = 5)
dim(imp)                                     # n x 5 completed exposures
}
\seealso{
\code{\link[=fit_shash_margin]{fit_shash_margin()}}, \code{\link[=draw_margin]{draw_margin()}}, \code{\link[=rnorm_trunc]{rnorm_trunc()}}, \code{\link[=gsimp_impute]{gsimp_impute()}}.
}
