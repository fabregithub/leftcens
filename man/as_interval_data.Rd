% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-cens.R
\name{as_interval_data}
\alias{as_interval_data}
\title{Create an interval-censored data object}
\usage{
as_interval_data(left, right, log_transform = TRUE)
}
\arguments{
\item{left, right}{Numeric vectors of interval lower and upper bounds on the
original measurement scale. Recycled to a common length. \code{left = 0} denotes
a non-detect lower bound; under \code{log_transform} it maps to \code{-Inf}, which is
expected, not an error.}

\item{log_transform}{Single logical. When \code{TRUE} (default), bounds are stored
on the natural-log scale, matching how the imputation and NPMLE machinery
operate. \code{left}/\code{right} must then be non-negative.}
}
\value{
A \code{cens_data} object: a list with elements \code{left}, \code{right},
\code{category} (a factor), and \code{log_transform}, with class \code{"cens_data"}.
}
\description{
Constructs a \code{cens_data} object from per-observation lower and upper interval
bounds. This is the single shared data representation consumed by every
descriptive-statistics and imputation function in \code{leftcens}, so the interval
semantics are defined exactly once.
}
\details{
Each observation is classified (see \code{cens_categories}) from its bounds:
\verb{(0, MDL)} is a non-detect, \verb{(MDL, LCMRL)} is detected-but-not-quantified, and
\verb{(value, value)} is a quantified point measurement. The classification is
derived, never supplied, so it cannot disagree with the bounds.

Classic single-limit (Helsel-style) left censoring is a degenerate case of
this same representation --- see \code{\link[=as_interval_data_from_single]{as_interval_data_from_single()}}.
}
\examples{
# non-detect, detected-not-quantified, quantified
as_interval_data(left = c(0, 1, 5), right = c(1, 3, 5))
}
