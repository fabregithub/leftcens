% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shash-margin.R
\name{draw_margin}
\alias{draw_margin}
\title{Draw a sinh-arcsinh margin's parameters from their asymptotic posterior}
\usage{
draw_margin(margin)
}
\arguments{
\item{margin}{A fit from \code{\link[=fit_shash_margin]{fit_shash_margin()}}.}
}
\value{
A list \verb{(mu, sigma, eps)}: one draw of the margin parameters (or the
plug-in estimate).
}
\description{
For \emph{proper} multiple imputation each completed dataset should condition on a
drawn margin, not the shared MLE, so that the between-imputation variance
reflects margin- and (extrapolated) tail-estimation uncertainty. Returns the
plug-in estimate when the fit carries no usable covariance (\code{margin$V} is
\code{NULL}) or the draw is degenerate, so behaviour is unchanged where the fit is
untrustworthy.
}
\examples{
set.seed(1)
x <- 2 + sinh(asinh(rnorm(200)) + 0.6)
fit <- fit_shash_margin(x, lower = numeric(0), upper = numeric(0))
draw_margin(fit)
}
\seealso{
\code{\link[=fit_shash_margin]{fit_shash_margin()}}.
}
