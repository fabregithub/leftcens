% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-cens.R
\name{as_interval_data_from_single}
\alias{as_interval_data_from_single}
\title{Create interval-censored data from single-limit (Helsel-style) input}
\usage{
as_interval_data_from_single(value, censored, log_transform = TRUE)
}
\arguments{
\item{value}{Numeric vector of measured values (for detected observations) or
detection limits (for censored observations).}

\item{censored}{Logical vector: \code{TRUE} where \code{value} is a detection limit
(non-detect), \code{FALSE} where it is a quantified measurement. Recycled against
\code{value}.}

\item{log_transform}{Passed to \code{\link[=as_interval_data]{as_interval_data()}}.}
}
\value{
A \code{cens_data} object.
}
\description{
A convenience wrapper around \code{\link[=as_interval_data]{as_interval_data()}} for datasets in the classic
single-detection-limit format: one value per observation plus a flag for
whether it was censored. This is the two-tier collapse of the interval model
(\code{non_detect} and \code{quantified} only), so it flows through the same
\code{cens_data} object as fully interval-censored data.
}
\examples{
# 2.0 is a non-detect at DL = 2.0; 5.1 is a quantified measurement
as_interval_data_from_single(value = c(2.0, 5.1), censored = c(TRUE, FALSE))
}
