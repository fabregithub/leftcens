% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-gsimp.R
\name{gsimp_impute}
\alias{gsimp_impute}
\title{Gibbs-sampler imputation of interval-censored data}
\usage{
gsimp_impute(
  x,
  iters_all = 10,
  iters_each = 1,
  initial = "bounds",
  imp_model = "copula",
  n_cores = 1,
  verbose = FALSE,
  margin_draw = TRUE
)
}
\arguments{
\item{x}{A \code{cens_bounds} object from \code{\link[=build_bounds]{build_bounds()}}, which supplies the
working-scale data matrix and the per-cell \verb{(lo, hi)} bound matrices.}

\item{iters_all}{Number of outer Gibbs sweeps over all analytes (the mixing /
convergence control). Each sweep updates every analyte once.}

\item{iters_each}{Inner refinement draws per analyte per sweep. This matters
for stochastic conditional models (e.g. cross-validated \code{glmnet}); for the
deterministic default it is redundant, hence the default of \code{1}.}

\item{initial}{Initialisation of censored cells: \code{"bounds"} (default; draw
from each analyte's observed mean/sd, truncated to the cell interval) or
\code{"qrilc"} (quantile-regression left-censored imputation, requires the
'imputeLCMD' package, then clamped into bounds).}

\item{imp_model}{Conditional model. \code{"copula"} (default) is the skew-robust
choice: it fits a flexible (sinh-arcsinh) margin per analyte, maps the data
to latent-Gaussian scores, runs an interval-censored Gaussian regression
there, and back-transforms -- keeping the mean near-unbiased under
right-skewed margins while matching \code{"tobit"} on log-normal data. It is a
\emph{proper} multiple-imputation engine (see \code{margin_draw}), and the slowest
option. \code{"tobit"} is an interval-censored Gaussian regression via
\code{\link[survival:survreg]{survival::survreg()}} fit on observed \emph{and} censored rows; fast-ish and
reliable \emph{when the margins are log-normal}, but biased under skew -- use it
for known-symmetric data or very large/wide problems. The observed-only
alternatives are \code{"ridge"} (base-R ridge regression, fastest and always
solvable), \code{"lm"}, and \code{"glmnet"} (elastic net, requires 'glmnet'). A custom
\verb{function(yo, Xo, Xm)} returning \code{list(mean, sd)} is also accepted. \code{"tobit"}
(and the latent step of \code{"copula"}) reduces the predictor dimension by PCA
when there are more analytes than samples, and falls back to \code{"ridge"} if the
censored fit cannot be formed.}

\item{n_cores}{Reserved for future parallel sweeps; currently only serial
execution is implemented (a value \verb{> 1} emits a message and runs serially).}

\item{verbose}{If \code{TRUE}, report progress per sweep.}

\item{margin_draw}{Only used by \code{imp_model = "copula"}. When \code{TRUE} (default),
each imputation draws the per-analyte margin parameters from their asymptotic
posterior, so pooling M such imputations propagates margin/tail-estimation
uncertainty into the multiple-imputation variance. Set \code{FALSE} to plug in the
MLE margin, giving the near-unbiased \emph{point} imputation. Recommended MI
workflow: the plug-in (\code{FALSE}) for the point estimate, drawn (\code{TRUE})
imputations for the between-imputation variance.}
}
\value{
A numeric matrix of the same dimensions as the input, with censored
cells filled in, on the same (working / log) scale as the \code{cens_bounds}
input. Observed cells are returned unchanged.
}
\description{
Imputes the censored cells of a wide analyte matrix by Gibbs sampling: each
analyte is repeatedly redrawn from its conditional distribution given the
other analytes, with every draw constrained to that cell's censoring interval
\verb{(lo, hi)} via a truncated normal (\code{\link[=rnorm_trunc]{rnorm_trunc()}}). This is a clean-room
implementation of the GSimp method (Wei et al. 2018), extended to per-cell
bounds so non-detect and detected-not-quantified cells of the same analyte
are imputed within their respective bands.
}
\examples{
a <- as_interval_data(c(0, 2, 5, 8, 3), c(1, 4, 5, 8, 3))
b <- as_interval_data(c(3, 0, 7, 2, 6), c(3, 1, 7, 4, 6))
bnds <- build_bounds(list(A = a, B = b))
filled <- gsimp_impute(bnds, iters_all = 5)
anyNA(filled)
}
\references{
Wei R. et al. (2018) GSimp: A Gibbs sampler based left-censored
missing value imputation approach for metabolomics studies. PLoS Comput.
Biol. 14(1):e1005973. \doi{10.1371/journal.pcbi.1005973}
}
\seealso{
\code{\link[=build_bounds]{build_bounds()}} for constructing the input.
}
