% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight.R
\name{preflight_from_data}
\alias{preflight_from_data}
\title{Pre-flight reliability check derived from your own data}
\usage{
preflight_from_data(x, imp_model = "tobit", iters_all = 10, ...)
}
\arguments{
\item{x}{A \code{cens_data} object or a list of them (one per analyte), as passed
to \code{\link[=build_bounds]{build_bounds()}}.}

\item{imp_model, iters_all}{Passed to \code{\link[=gsimp_impute]{gsimp_impute()}} for both the
parameter-estimation impute and the pre-flight.}

\item{...}{Further arguments passed to \code{\link[=preflight_reliability]{preflight_reliability()}} (e.g.
\code{n_rep}, \code{M}, \code{seed}).}
}
\value{
A \code{preflight} object; see \code{\link[=preflight_reliability]{preflight_reliability()}}.
}
\description{
A convenience wrapper around \code{\link[=preflight_reliability]{preflight_reliability()}} that reads the study
design directly from your \code{cens_data}: it takes the per-analyte non-detect
fractions from the data, fits each analyte's log-scale margin (location,
spread, and \strong{skewness}) by interval-censored MLE, and estimates the
correlation from a quick imputation --- all used only to parameterise the
simulation. It then runs the pre-flight for that design. Estimating the skew
matters, and from the censored likelihood rather than the imputed values: a
log-normal assumption would make the report overly optimistic for skewed
analytes, and a symmetric imputation would hide the skew. Pass an explicit
\code{skew} to override the estimate.
}
\details{
Because the moments and correlation are estimated (in part from imputed
values), treat the result as an approximate guide to what the imputation can
deliver for data like yours.
}
\examples{
\donttest{
a <- as_interval_data(c(0, 2, 5, 8, 3), c(1, 4, 5, 8, 3))
b <- as_interval_data(c(3, 0, 7, 2, 6), c(3, 1, 7, 4, 6))
preflight_from_data(list(a = a, b = b), n_rep = 10, M = 5)
}
}
