% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shash-margin.R
\name{fit_shash_margin}
\alias{fit_shash_margin}
\title{Fit a sinh-arcsinh margin by interval-censored maximum likelihood}
\usage{
fit_shash_margin(observed, lower, upper)
}
\arguments{
\item{observed}{Numeric vector of exactly observed values.}

\item{lower, upper}{Lower/upper bounds of the censored cells (same length as
each other). \code{lower} may be \code{-Inf} for left-censored non-detects and \code{upper}
may be \code{Inf} for right-censored values. Pass length-0 vectors when there are
no censored cells.}
}
\value{
A list with the point estimate (\code{mu}, \code{sigma}, \code{eps}), the fitted
parameter vector \verb{par = c(mu, log sigma, eps)}, and its asymptotic covariance
\code{V} (inverse observed information), or \code{V = NULL} when it cannot be formed
(which makes \code{\link[=draw_margin]{draw_margin()}} fall back to the plug-in estimate). Falls back to
a Gaussian \verb{(mean, sd, 0)} when there is too little data or the optimiser
fails, so it degrades gracefully to tobit.
}
\description{
Estimates the location, scale, and skewness of a 3-parameter sinh-arcsinh
margin from a mix of exactly observed values and interval-censored cells
(including left-censored non-detects). The fit is skew-aware, so non-detects
are later drawn from a correctly shaped tail rather than a Gaussian one.
}
\examples{
set.seed(1)
x <- 2 + sinh(asinh(rnorm(200)) + 0.6)      # right-skewed
lod <- quantile(x, 0.3)
obs <- x[x >= lod]
fit <- fit_shash_margin(obs, lower = rep(-Inf, sum(x < lod)),
                        upper = rep(lod, sum(x < lod)))
fit[c("mu", "sigma", "eps")]
}
\seealso{
\code{\link[=draw_margin]{draw_margin()}} for a proper-MI parameter draw; \code{\link[=x_to_z]{x_to_z()}} /
\code{\link[=z_to_x]{z_to_x()}} for the transforms; \code{\link[=impute_censored_conditional]{impute_censored_conditional()}}.
}
