% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{surfacewater}
\alias{surfacewater}
\title{Surface-water trace-metal concentrations (flagged layout)}
\format{
A data frame with 30 rows and 11 columns:
\describe{
\item{sample_id}{sample identifier}
\item{As, As.flag}{arsenic value (ug/L) and qualifier flag}
\item{Cd, Cd.flag}{cadmium value (ug/L) and qualifier flag}
\item{Pb, Pb.flag}{lead value (ug/L) and qualifier flag}
\item{Cu, Cu.flag}{copper value (ug/L) and qualifier flag}
\item{Zn, Zn.flag}{zinc value (ug/L) and qualifier flag}
}
}
\source{
Simulated for illustration; see \code{data-raw/sample_data.R}.
}
\usage{
surfacewater
}
\description{
A small simulated example in the \strong{two-tier value + qualifier-flag} layout,
the most common laboratory export: one row per sample, and for each metal a
value column plus a \code{.flag} column where \code{"<"} marks a non-detect (the value
being the detection/reporting limit) and a blank marks a quantified result.
Concentrations are in micrograms per litre. Read it with
\code{as_cens_list(surfacewater, format = "flagged")}.
}
\examples{
cl <- as_cens_list(surfacewater, format = "flagged")
as.data.frame(desc_np(cl))
}
\seealso{
\link{groundwater} for the interval (\code{.left}/\code{.right}) layout.
}
\keyword{datasets}
