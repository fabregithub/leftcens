% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shash-margin.R
\name{x_to_z}
\alias{x_to_z}
\alias{z_to_x}
\title{Sinh-arcsinh transforms between the data scale and a latent normal scale}
\usage{
x_to_z(x, mu, sigma, eps)

z_to_x(z, mu, sigma, eps)
}
\arguments{
\item{x, z}{Numeric vectors on the data scale (\code{x}) or latent normal scale (\code{z}).}

\item{mu, sigma, eps}{Margin parameters: location, scale (> 0), and skewness
(\code{eps = 0} gives a plain Gaussian margin). Typically from \code{\link[=fit_shash_margin]{fit_shash_margin()}}
or a draw from \code{\link[=draw_margin]{draw_margin()}}.}
}
\value{
A numeric vector the same length as the input.
}
\description{
\code{x_to_z()} maps data to its latent standard-normal score and \code{z_to_x()} is the
exact inverse (the marginal quantile map \eqn{F^{-1}(\Phi(\cdot))}). Both are
monotone increasing, so censoring bounds transform through unchanged (and
\code{-Inf} / \code{Inf} endpoints map to themselves).
}
\examples{
z <- x_to_z(c(1, 2, 5), mu = 2, sigma = 1, eps = 0.5)
z_to_x(z, mu = 2, sigma = 1, eps = 0.5)   # recovers c(1, 2, 5)
}
\seealso{
\code{\link[=fit_shash_margin]{fit_shash_margin()}}, \code{\link[=draw_margin]{draw_margin()}}, \code{\link[=impute_censored_conditional]{impute_censored_conditional()}}
}
