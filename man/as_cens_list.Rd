% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cens-frame.R
\name{as_cens_list}
\alias{as_cens_list}
\title{Build a list of \code{cens_data} from a wide data frame}
\usage{
as_cens_list(
  data,
  analytes = NULL,
  format = c("left_right", "flagged"),
  left_suffix = ".left",
  right_suffix = ".right",
  flag_suffix = ".flag",
  nd_flags = c("<", "ND", "U"),
  log_transform = TRUE
)
}
\arguments{
\item{data}{A data frame (or object coercible to one).}

\item{analytes}{Optional character vector of analyte names. When \code{NULL}
(default) they are inferred from the column names for the chosen \code{format}.}

\item{format}{\code{"left_right"} (columns \verb{<analyte><left_suffix>} /
\verb{<analyte><right_suffix>}) or \code{"flagged"} (columns \verb{<analyte>} /
\verb{<analyte><flag_suffix>}).}

\item{left_suffix, right_suffix}{Column-name suffixes for the \code{"left_right"}
format.}

\item{flag_suffix}{Column-name suffix for the qualifier flag in the
\code{"flagged"} format.}

\item{nd_flags}{Flag values that mark a non-detect in the \code{"flagged"} format.}

\item{log_transform}{Passed to \code{\link[=as_interval_data]{as_interval_data()}}.}
}
\value{
A named list of \code{cens_data} objects, with class \code{"cens_list"}.
}
\description{
Turns a wide laboratory-style table --- one row per sample, one or two columns
per analyte --- into a named list of \code{\link[=as_interval_data]{as_interval_data()}} objects, one per
analyte. That list is exactly what \code{\link[=desc_np]{desc_np()}}, \code{\link[=desc_sp]{desc_sp()}}, and
\code{\link[=build_bounds]{build_bounds()}} accept, so this is the single on-ramp from a raw data frame to
every part of the package.
}
\details{
Two common layouts are supported:

\describe{
\item{\code{"left_right"}}{Each analyte has a lower- and upper-bound column,
e.g. \code{A.left} / \code{A.right}. This carries the full three-tier structure
(non-detect, detected-not-quantified, quantified).}
\item{\code{"flagged"}}{Each analyte has a value column and a qualifier-flag
column, e.g. \code{A} / \code{A.flag}, where a flag in \code{nd_flags} marks a non-detect
(the value being the detection/reporting limit) and any other flag a
quantified result. This two-tier layout is the most common lab export.}
}
}
\examples{
# left/right layout
d <- data.frame(ID = 1:3, A.left = c(0, 2, 5), A.right = c(1, 4, 5),
                B.left = c(3, 0, 7), B.right = c(3, 1, 7))
as_cens_list(d)

# flagged layout
f <- data.frame(ID = 1:3, A = c(2.1, 0.5, 3.4), A.flag = c("", "<", ""))
as_cens_list(f, format = "flagged")
}
\seealso{
\code{\link[=as_interval_data]{as_interval_data()}} for a single analyte; the "Preparing your own
data" vignette.
}
