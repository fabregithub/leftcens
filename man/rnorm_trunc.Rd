% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-gsimp.R
\name{rnorm_trunc}
\alias{rnorm_trunc}
\title{Draw from a truncated normal distribution}
\usage{
rnorm_trunc(n, mean, sd, lower, upper)
}
\arguments{
\item{n}{Number of draws.}

\item{mean, sd}{Mean and standard deviation of the untruncated normal.}

\item{lower, upper}{Truncation bounds (may be \code{-Inf} / \code{Inf}).}
}
\value{
A numeric vector of length \code{n}.
}
\description{
Samples from \code{N(mean, sd^2)} conditioned to lie in \verb{(lower, upper)}, using
inverse-CDF sampling. One-sided-infinite intervals (e.g. the \verb{(-Inf, log MDL)}
bound of a log-scale non-detect) are handled on the log-probability scale so
draws deep in a tail do not underflow to \verb{+/-Inf}. Arguments are recycled to
length \code{n}.
}
\examples{
# left-censored: values known only to be below log(2)
summary(rnorm_trunc(1000, mean = 1, sd = 1, lower = -Inf, upper = log(2)))
}
