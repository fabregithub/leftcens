% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-bounds.R
\name{build_bounds}
\alias{build_bounds}
\title{Build per-cell imputation bounds from interval-censored data}
\usage{
build_bounds(x)
}
\arguments{
\item{x}{A single \code{cens_data} object (one analyte column) or a list of
\code{cens_data} objects, one per analyte. When a list, all elements must have
the same length and the same \code{log_transform} flag; list names become the
output column names.}
}
\value{
A \code{cens_bounds} object: a list with numeric matrices \code{data_wide},
\code{lo_mat}, and \code{hi_mat} (all \code{n} observations x \code{p} analytes), a logical
matrix \code{to_impute}, and the \code{log_transform} flag.
}
\description{
Converts one or more \code{cens_data} columns into the per-cell bound matrices
consumed by \code{gsimp_impute()} (the imputation module). Each analyte column
becomes a column of three
aligned matrices: the observed values (\code{data_wide}, with censored cells
\code{NA}), and the lower/upper bounds (\code{lo_mat} / \code{hi_mat}, \code{NA} at observed
cells). Building all three together guarantees the imputation input and its
bounds share exactly the same missing-cell pattern.
}
\details{
All matrices are on the stored scale of the input (log scale when the
\code{cens_data} objects were built with \code{log_transform = TRUE}, which is the
scale the Gibbs sampler operates on). On the log scale a non-detect \verb{(0, MDL)}
becomes the left-censored bound \verb{(-Inf, log MDL)} directly, with no special
casing. Cells whose bounds are both \code{NA} are treated as ordinary
(unconstrained) missing values and imputed from \verb{(-Inf, Inf)}.
}
\examples{
a <- as_interval_data(left = c(0, 2, 5), right = c(1, 4, 5))   # analyte A
b <- as_interval_data(left = c(3, 0, 7), right = c(3, 1, 7))   # analyte B
bnds <- build_bounds(list(A = a, B = b))
bnds$lo_mat
}
\seealso{
\code{\link[=as_interval_data]{as_interval_data()}} for building the \code{cens_data} inputs.
}
