% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc-semiparam.R
\name{desc_sp}
\alias{desc_sp}
\title{Semi-parametric summary of interval-censored data}
\usage{
desc_sp(x, covariates = NULL, model = c("ph", "po"), bs_samples = 0)
}
\arguments{
\item{x}{A \code{cens_data} object (see \code{\link[=as_interval_data]{as_interval_data()}}) for a single
analyte, \strong{or} a named list / \code{cens_list} of them (see \code{\link[=as_cens_list]{as_cens_list()}})
to fit several analytes against the same covariates at once.}

\item{covariates}{Optional covariates aligned to \code{x} (one row/element per
observation): a data frame, an atomic vector, a factor, or \code{NULL} for an
intercept-only baseline model.}

\item{model}{\code{"ph"} (proportional hazards, the default) or \code{"po"}
(proportional odds).}

\item{bs_samples}{Number of bootstrap samples for coefficient standard errors
and p-values. The default \code{0} fits point estimates only (fast and
deterministic); set it higher for inference via \code{\link[=summary]{summary()}}.}
}
\value{
For a single \code{cens_data}, a \code{cens_sp_fit} object (fitted \code{fit},
\code{model}, \code{covariates} names, \code{log_transform}, \code{n}, \code{dropped}, \code{category}
counts). For a list of analytes, a \code{cens_sp_fits} collection with \code{print()},
\code{as.data.frame()}, \code{tidy()}, and \code{glance()} methods that stack the analytes'
coefficient tables.
}
\description{
Fits a semi-parametric proportional-hazards or proportional-odds model to a
\code{cens_data} object with a non-parametric baseline, via \code{\link[icenReg:ic_sp]{icenReg::ic_sp()}}.
With no covariates this is a semi-parametric estimate of the baseline
distribution; with covariates it additionally estimates how each covariate
shifts the concentration distribution (as a hazard ratio under \code{"ph"} or an
odds ratio under \code{"po"}).
}
\details{
Like \code{\link[=desc_np]{desc_np()}}, the model is fit on the original measurement scale, so
baseline curves and quantiles are expressed directly as concentrations.
Covariates are matched to \code{x} by position and are subset in step with any
observations dropped for missing bounds.
}
\examples{
x <- as_interval_data(left = c(0, 1, 5, 8, 0, 2), right = c(1, 3, 5, 8, 1, 4))
grp <- factor(c("a", "a", "a", "b", "b", "b"))
fit <- desc_sp(x, covariates = grp, model = "ph")
fit
}
\seealso{
\code{\link[=plot.cens_sp_fit]{plot.cens_sp_fit()}} for baseline / per-group distribution curves,
and \code{\link[=as_cens_list]{as_cens_list()}} to build the multi-analyte input from a data frame.
}
