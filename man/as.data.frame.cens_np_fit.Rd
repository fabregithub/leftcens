% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc-nonparam.R
\name{as.data.frame.cens_np_fit}
\alias{as.data.frame.cens_np_fit}
\title{Coerce a non-parametric NPMLE fit to a data frame}
\usage{
\method{as.data.frame}{cens_np_fit}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  probs = c(0.1, 0.25, 0.5, 0.75, 0.9),
  ql = x$quantitation_limit
)
}
\arguments{
\item{x}{A \code{cens_np_fit} object.}

\item{row.names, optional}{Passed along for S3 consistency; \code{row.names} sets
the row names if supplied.}

\item{...}{Unused.}

\item{probs}{Probabilities to tabulate.}

\item{ql}{Quantitation-limit threshold (see \code{\link[=quantile.cens_np_fit]{quantile.cens_np_fit()}}).}
}
\value{
A data frame with columns \code{probability}, \code{quantile}, \code{estimate}.
}
\description{
Returns the quantile summary as a tidy data frame --- one row per requested
probability, with columns \code{probability}, \code{quantile} (the \code{"10\%"}-style label),
and \code{estimate} --- ready to hand to a table-formatting package such as \code{gt},
\code{flextable}, \code{knitr::kable()}, or \code{DT}. Estimates below the quantitation limit
are \code{NA} (see \code{\link[=quantile.cens_np_fit]{quantile.cens_np_fit()}}); the detection and quantitation limits
are attached as attributes.
}
\examples{
fit <- desc_np(as_interval_data(c(0, 1, 5, 8), c(1, 3, 5, 8)))
as.data.frame(fit)
# e.g. knitr::kable(as.data.frame(fit))  or  gt::gt(as.data.frame(fit))
}
