% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-mi.R
\name{gsimp_mi}
\alias{gsimp_mi}
\title{Multiple imputation of censored data with pooled inference}
\usage{
gsimp_mi(
  x,
  m = 20L,
  imp_model = "copula",
  iters_all = 10L,
  initial = "bounds",
  conf = 0.95,
  adaptive = FALSE,
  m_max = 100L,
  m_step = 10L,
  tol = 0.01,
  return_imputations = FALSE,
  n_cores = 1L,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{cens_bounds} object from \code{\link[=build_bounds]{build_bounds()}}.}

\item{m}{Number of imputations (the starting number when \code{adaptive = TRUE}).}

\item{imp_model, iters_all, initial}{Passed to \code{\link[=gsimp_impute]{gsimp_impute()}}.}

\item{conf}{Confidence level for the pooled interval.}

\item{adaptive}{If \code{TRUE}, add imputations in batches of \code{m_step} until the
mean interval width changes by less than \code{tol} (relative) or \code{m_max} is hit.}

\item{m_max, m_step, tol}{Adaptive-mode controls.}

\item{return_imputations}{If \code{TRUE}, keep the completed datasets in the result
(the proper drawn-margin set for the copula) for a downstream analysis.}

\item{n_cores}{Fork workers for parallel imputation (serial on Windows or when
\code{1}).}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A \code{gsimp_mi} object: \code{pooled} (per-analyte estimate, variances, df,
CI, and \code{fmi}), the \code{m} used, the adaptive \code{path}, and optionally
\code{imputations}.
}
\description{
Generates several imputations with \code{\link[=gsimp_impute]{gsimp_impute()}} and pools the per-analyte
means by Rubin's rules, so the imputation uncertainty is carried into the
standard errors (a single imputation understates it). Reports the fraction of
missing information (FMI) per analyte, which governs how many imputations are
needed. With \code{adaptive = TRUE} it increases \code{m} in steps until the pooled
interval width stabilises --- useful because the required \code{m} falls with sample
size for a proper model (the imputation-model uncertainty shrinks as the fit
tightens), so a fixed \code{m} is wasteful for large \code{n} and risky for small \code{n}.
}
\details{
For \code{imp_model = "copula"} the recommended hybrid is used automatically: the
point estimate comes from plug-in-margin imputations and the between-imputation
variance from drawn-margin imputations, so intervals stay calibrated without a
point bias.
}
\examples{
\donttest{
a <- as_interval_data(c(0, 2, 5, 8, 3), c(1, 4, 5, 8, 3))
b <- as_interval_data(c(3, 0, 7, 2, 6), c(3, 1, 7, 4, 6))
fit <- gsimp_mi(build_bounds(list(A = a, B = b)), m = 10)
fit$pooled
}
}
\seealso{
\code{\link[=gsimp_impute]{gsimp_impute()}} for a single imputation.
}
