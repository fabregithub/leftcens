% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc-semiparam.R
\name{as.data.frame.cens_sp_fit}
\alias{as.data.frame.cens_sp_fit}
\title{Coerce a semi-parametric fit to a data frame}
\usage{
\method{as.data.frame}{cens_sp_fit}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{cens_sp_fit} object.}

\item{row.names, optional}{Passed along for S3 consistency.}

\item{...}{Unused.}
}
\value{
A data frame, one row per covariate coefficient.
}
\description{
Returns the covariate coefficient table as a tidy data frame --- \code{term},
\code{coef} (log-scale), and the exponentiated \code{hazard_ratio} (PH) or \code{odds_ratio}
(PO), plus \code{std_error} and \code{p_value} when the fit was built with
\code{bs_samples > 0} --- ready to hand to a table-formatting package. An
intercept-only (no-covariate) fit yields a zero-row frame.
}
\examples{
x <- as_interval_data(c(0, 1, 5, 8, 0, 2), c(1, 3, 5, 8, 1, 4))
grp <- factor(c("a", "a", "a", "b", "b", "b"))
as.data.frame(desc_sp(x, covariates = grp, model = "ph"))
}
