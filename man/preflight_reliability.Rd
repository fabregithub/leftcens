% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight.R
\name{preflight_reliability}
\alias{preflight_reliability}
\title{Pre-flight reliability check for a study design}
\usage{
preflight_reliability(
  n,
  nd_frac,
  p = NULL,
  rho = 0.5,
  dnq_frac = 0,
  skew = 0,
  mu = NULL,
  sd = 1,
  n_rep = 30,
  M = 20,
  iters_all = 10,
  imp_model = "tobit",
  bias_tol = 0.1,
  coverage_tol = 0.9,
  seed = NULL
)
}
\arguments{
\item{n}{Number of samples (rows) in the design.}

\item{nd_frac}{Non-detect fraction, as a single value applied to all analytes
or a length-\code{p} vector of per-analyte fractions.}

\item{p}{Number of analytes. Defaults to \code{length(nd_frac)} when that is a
vector.}

\item{rho}{Exchangeable correlation between analytes on the log scale, in
\verb{[0, 1)}.}

\item{dnq_frac}{Detected-not-quantified fraction (the middle tier), scalar or
length-\code{p}. Default \code{0} (pure left-censoring).}

\item{skew}{sinh-arcsinh skewness of the marginals (\code{0} = log-normal).}

\item{mu, sd}{Log-scale mean and standard deviation of the analytes; scalar or
length-\code{p}. Defaults span a modest range of means at unit sd.}

\item{n_rep}{Number of simulated datasets.}

\item{M}{Number of imputations per dataset for the coverage estimate.}

\item{iters_all}{Gibbs sweeps per imputation (see \code{\link[=gsimp_impute]{gsimp_impute()}}).}

\item{imp_model}{Conditional model (see \code{\link[=gsimp_impute]{gsimp_impute()}}); default \code{"tobit"}.}

\item{bias_tol, coverage_tol}{Reliability thresholds: maximum |mean bias| and
minimum coverage.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A \code{preflight} object: a list with \code{by_analyte} (a data frame of
per-analyte \code{nd_frac}, \code{mean_bias}, \code{median_bias}, \code{rmse}, \code{coverage}, and
\code{reliable}) and the \code{config} used.
}
\description{
Simulates many datasets matching a described study design (sample size,
per-analyte non-detect fractions, correlation, ...), imputes each with
\code{\link[=gsimp_impute]{gsimp_impute()}}, and reports the bias and multiple-imputation coverage you
can expect --- so imputation can be vetted \emph{before} it is trusted on real
data. An analyte is flagged \code{reliable} when its mean bias is small, its 95\%
interval covers the truth often enough, and its non-detect fraction is below
50\% (the point above which the median itself is censored).
}
\details{
This is a Monte Carlo procedure and takes some time (it fits \code{n_rep * (M + 1)}
imputations). It assumes an approximately log-normal, exchangeably-correlated
data-generating process; use it as a guide, not a guarantee.
}
\examples{
\donttest{
# three analytes at 30\% / 45\% / 55\% non-detects, n = 120
preflight_reliability(n = 120, nd_frac = c(0.30, 0.45, 0.55),
                      n_rep = 10, M = 5)
}
}
\seealso{
\code{\link[=preflight_from_data]{preflight_from_data()}} to derive the design from your own data.
}
